Config(
  width: Absolute(600),
  position: Center,
  vertical_offset: Absolute(-250),
  hide_icons: true,
  ignore_exclusive_zones: true,
  layer: Overlay,
  hide_plugin_info: true,
  plugins: [
    "libapplications.so",
    "libshell.so",
    "librink.so",
  ],
)
